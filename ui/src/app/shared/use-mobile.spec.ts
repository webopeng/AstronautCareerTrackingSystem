import { UseMobile } from './use-mobile';

describe('UseMobile', () => {
  it('should create an instance', () => {
    const directive = new UseMobile();
    expect(directive).toBeTruthy();
  });
});
